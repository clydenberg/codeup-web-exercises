(function(){
    "use strict";

    // var planetsString = "Mercury|Venus|Earth|Mars|Jupiter|Saturn|Uranus|Neptune";
    // var planetsArray;

    /**
     * TODO:
     * Convert planetsString to an array, and save it in a variable named
     * planetsArray.
     * console.log planetsArray to check your work
     */

    // planetsArray = planetsString.split(`|`)
    // console.log(planetsArray);

    /**
     * TODO:
     * Create a string with <br> tags between each planet. console.log() your
     * results. Why might this be useful?
     */

    // planetsArray = planetsArray.join('<br>');
    // console.log(planetsArray);

        {/*BONUS: */}
        {/*Create another string that would display your planets in an undordered*/}
        {/*list. You will need an opening AND closing <ul> tags around the entire */}
        {/*    string, and <li> tags around each planet.*/}


    //I TRIED TO FILL IN THIS ONLINE EXAMPLE TO FIT OUR ARRAY

    // const bonusPlanets = (planet)=>{
    //         let list = document.createElement(`ul`);
    //
    //         for(let i = 0; i < planet.length; i++){
    //             [i] = '<li>' + planet[0][i] + '</li>';
    //             let item = document.createElement(`li`);
    //             item.appendChild(document.createTextNode(planet[i]));
    //             list.appendChild(item);
    //             //document.getElementById('foo').appendChild(makeUL(options[0]));
    //         }
    //         return list;
    // }
    // document.getElementById('#foo').appendChild(makeUL(options[0]));
    //  console.log(bonusPlanets(planetsString));

    //is there a way to feed a function an array as an argument and have the function convert it into a unordered list










})();

