(()=>{

    /*we are saving a node to a variable so we can apply methods to it*/
    const pageHeader = document.querySelector(`header.container h1`);

    /*example of node method being applied to a saved node*/
    pageHeader.style.color = `red`;



})();